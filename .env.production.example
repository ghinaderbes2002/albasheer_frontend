# Copy this file to `.env` (next to docker-compose.yml) and adjust the values
# for your deployment environment, then run `docker compose up -d`.
#
# IMPORTANT: these are PUBLIC values inlined into the JavaScript bundle.
# They must be reachable by the end user's browser.

VITE_API_BASE_URL=http://161.97.156.42:8000
VITE_MEDIA_BASE_URL=http://161.97.156.42:8000
VITE_DEFAULT_LANG=ar
