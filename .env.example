# Backend API base URL — JWT auth + REST endpoints live here
VITE_API_BASE_URL=http://localhost

# Where /media/... assets are served from. Usually same as API host.
VITE_MEDIA_BASE_URL=http://localhost

# Default UI language: ar | en
VITE_DEFAULT_LANG=ar
